import { Fragment } from "react";
import classes from './NavFooter.module.scss';

const NavFooter = (props: any) => {
    
  
    
    return (
     <div className={classes.footer}>
        <div className={classes.dec}>
        <div>Await the Ascension!!</div>
        </div>
       
     </div>
    );
}

export default NavFooter;